App(
    appid="fuse_radio",
    name="Fuse Radio",
    apptype=FlipperAppType.EXTERNAL,
    entry_point="fuse_radio_app",
    requires=["gui"],
    stack_size=4 * 1024,
    fap_category="GPIO",
    fap_version=(0, 1),
    fap_description="ESP32 companion for WiFi / BLE / ZigBee tools",
)
